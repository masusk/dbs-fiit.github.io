---
layout: dbs
title:  "Prednášky"
---
<div class="row">
     <div id="sidebar" class="col-sm-3">
	<ul class="well nav nav-pills nav-stacked affix">
          <li class="nav-header">Prednášky</li>
  {% for post in site.posts reversed %}
     {% if post.categories contains 'lectures' %}
            <li><a href="#{{post.anchor}}">{{post.title}}</a></li>
     {% endif %}
  {% endfor %}
	</ul>
     </div>
     <div class="col-sm-9">
  {% for post in site.posts reversed %}
     {% if post.categories contains 'lectures' %}
	<div id="{{post.anchor}}" class="row">
	  <div class="col-sm-7">
		{{ post.content }}
          </div>
	</div>
      {% endif %}
   {% endfor %}
     </div> <!-- col-sm-10 -->
</div> <!-- row -->

